import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const FAQ = () => {
  return (
    <SafeAreaView>
      <Text className="text-xl text-primary">FAQ</Text>
    </SafeAreaView>
  );
};

export default FAQ;
